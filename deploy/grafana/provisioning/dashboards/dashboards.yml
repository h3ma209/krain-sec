apiVersion: 1

providers:
  - name: Krain Honeypot
    orgId: 1
    folder: Krain
    folderUid: krain
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
